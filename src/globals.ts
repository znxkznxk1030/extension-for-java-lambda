export interface IconPaths {
  help: string;
  lambda: string;
  settings: string;
  registry: string;
  s3: string;
  folder: string;
  file: string;
  bucket: string;
  policy: string;
  plus: string;
  edit: string;
  exit: string;
  refresh: string;
}
